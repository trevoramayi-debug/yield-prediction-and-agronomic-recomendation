__pycache__/
*.pyc

# Local environment configuration. Ignored per the convention set on main.
# Copy .env.example to .env and it works -- the example carries the Earth
# Engine project ID, which is not a credential.
.env
.env.local

# Credentials, belt and braces. The service-account key is stored OUTSIDE this
# repository (~/.config/earthengine/) and .env holds only a path to it, so
# nothing below should ever match anything. They are here so that if someone
# later drops a key file into the working tree -- which is what happens under
# deadline -- git refuses to stage it instead of silently publishing it.
secrets/
*-service-account*.json
*service_account*.json
*credentials*.json
*.pem
*.key

# Earth Engine resume cache: one CSV per fetched (year, month), written by
# scripts/fetch_gee_weather.py purely so an interrupted fetch can restart where
# it stopped. Fully reproducible from Earth Engine, and ~700 tiny files.
# The committed artefacts are the assembled panel and climatology beside it.
data/weather/_gee_cache/

# Serialised model artefacts written by scripts/train_district_model.py. Each is
# 90-125 MB of fitted forests and network weights -- above GitHub's 100 MB file
# limit, and regenerated in ~4 minutes from the recipe that IS committed
# (data/models/06_final_model_config.json) with:
#   python scripts/train_district_model.py --out data/models/district_v1
# The metadata.json beside each artefact is small and stays tracked, so the
# training seasons and measured accuracy of a build remain in the history.
data/models/**/pipeline.joblib

# Python virtual environments.
.venv/
venv/

# Pytest / tooling caches.
.pytest_cache/

# Packaged model artefacts written by scripts/package_model_artifact.py, ready
# to attach to a release. Same reason as the pipeline.joblib above: ~120 MB.
dist/
node_modules/

